# Rally — Design System

리듬 탁구 XR 앱 **Rally**의 디자인 시스템. 이 문서의 모든 값은 실제 코드
(`src/app/globals.css`, `src/components/ui.tsx`)에서 추출한 것이며, 임의로
지어낸 수치는 없다.

다른 화면이나 웹사이트로 이 시스템을 확장·변주할 때 참조하도록 만들었다.
마지막 장(**베리에이션 가이드**)에 "바꿔도 되는 것"과 "바꾸면 시스템이
무너지는 것"을 구분해 두었다.

---

## 1. 원칙

이 시스템은 세 갈래에서 왔고, 각각 **가져온 층위가 다르다.**

| 출처 | 가져온 것 | 가져오지 않은 것 |
|---|---|---|
| Nike | 타이포 위계, 대문자 디스플레이, 엣지투엣지 플랫 리스트, 알약 CTA | 컬러 |
| Spotify | 박스 레이아웃의 조밀한 라운드(4/6/8), 그림자 깊이 | 컬러(전부 achromatic) |
| Rally 고유 | 브랜드 오렌지, 모션 곡선, 다크모드 팔레트 | — |

여기서 따라오는 규칙:

1. **색은 하나만 쓴다.** 화면의 색 언어는 "무채색 + 오렌지 액센트" 하나뿐이다.
   파랑·초록 같은 색을 추가하면 액센트가 액센트가 아니게 된다.
   (`--color-success`/`--color-info`는 상태 표시 전용이며 장식에 쓰지 않는다.)
2. **큰 글자는 좁게, 작은 글자는 넓게.** 옵티컬 보정 원칙을 자간에 그대로 적용한다.
3. **리스트는 평평하게.** 행(Row)은 카드가 아니라 헤어라인으로 구분한다. 라운드를
   주지 않는다.
4. **누를 수 있어 보이면 눌려야 한다.** `Row`에 `onClick`이 없으면 chevron도
   탭 피드백도 붙이지 않는다. 실제로 이걸 어겨서 "가짜 링크" 버그가 났었다.
5. **모션은 하나의 곡선을 공유한다.** 특별한 이유가 없으면
   `cubic-bezier(0.16, 1, 0.3, 1)`.

---

## 2. 컬러

CSS 변수는 Tailwind `@theme`에 선언돼 `bg-canvas` / `text-ink` 같은 유틸리티로
바로 쓰인다.

### 브랜드

| 토큰 | Light | 용도 |
|---|---|---|
| `--color-primary` | `#f24822` | 액센트, 이월 라벨, 활성 상태, 판정 강조 |
| `--color-primary-deep` | `#c9330f` | 눌림/강조 변형 |
| `--color-primary-soft` | `#ffe9e3` | 아이콘 배경 등 옅은 면 |
| `--color-on-primary` | `#ffffff` | primary 면 위의 텍스트 |

### 중립 (Nike 스케일)

| 토큰 | Light | Dark | 용도 |
|---|---|---|---|
| `--color-canvas` | `#ffffff` | `#111111` | 페이지 배경 |
| `--color-cloud` | `#f5f5f5` | `#1c1c1c` | 타일·입력창·보조 버튼 면 |
| `--color-ink` | `#111111` | `#f5f5f5` | 본문/제목 |
| `--color-charcoal` | `#39393b` | `#d8d8d8` | 짙은 보조 텍스트 |
| `--color-ash` | `#4b4b4d` | `#b6b6b6` | 보조 텍스트 |
| `--color-mute` | `#707072` | `#92928f` | 캡션, 설명문 |
| `--color-stone` | `#9e9ea0` | `#6b6b6b` | 플레이스홀더, chevron |
| `--color-hairline` | `#cacacb` | `#3a3a3a` | 테두리(진한 쪽) |
| `--color-hairline-soft` | `#e5e5e5` | `#262626` | 리스트 구분선 |

### 상태

| 토큰 | Light | Dark |
|---|---|---|
| `--color-success` | `#007d48` | — |
| `--color-success-bright` | `#1eaa52` | `#2fc86a` |
| `--color-info` | `#1151ff` | — |

### 다크모드 방식

`:root[data-theme="dark"]`에서 **토큰만 덮어쓴다.** 컴포넌트 코드에는
다크모드 분기가 없다. 첫 페인트 전에 인라인 스크립트가 `localStorage`를 읽어
`data-theme`을 심어 FOUC를 막는다.

> **예외 — 항상 어두운 면.** 앨범아트 위 캠페인 카드, XR HUD처럼 사진/영상 위에
> 얹히는 표면은 테마와 무관하게 항상 검다. 이런 곳은 토큰 대신 리터럴
> `black`/`white`를 쓴다. 토큰을 쓰면 다크모드에서 뒤집혀 흰 카드가 된다.

---

## 3. 타이포그래피

### 폰트

| 역할 | 폰트 | 비고 |
|---|---|---|
| 디스플레이(영문) | **Bebas Neue** 400 | Nike Futura 대체 |
| 디스플레이(한글) | **Pretendard** 900 | Bebas에 한글 글리프가 없어 자동 폴백 |
| 본문 전체 | **Pretendard** variable 45–920 | |

### 티어

| 클래스 | line-height | letter-spacing | 기타 |
|---|---|---|---|
| `.type-display` | 1.2 | `-0.02em` | `uppercase`, weight 900 |
| `.type-display-sm` | — | `-0.01em` | 32px 안팎 숫자용 |
| `.type-eyebrow` | 1.4 | `0.12em` | 대문자 소형 라벨 |
| `.type-caption` | 1.4 | `0.01em` | 11–13px 메타 |
| (본문 기본) | 1.5 | `0` | `body`에 선언 |

### 실제 쓰이는 크기

| px | 쓰임 |
|---|---|
| 44 | 페이지 타이틀 (`PageHeader`) |
| 40 / 36 | 히어로 카드 제목 |
| 32 | 스탯 숫자 (`StatTile`) |
| 16 | 섹션 제목 (semibold) |
| 15 | 리스트 행 라벨, 버튼 |
| 14 | 설명문, 행 값 |
| 12–13 | 캡션, 칩, 이월 라벨 |
| 11 | 최소 메타 |

### 한글 조판

```css
word-break: keep-all;      /* 어절 단위 줄바꿈 — 음절 중간에서 안 끊김 */
overflow-wrap: break-word; /* URL 등 진짜 못 끊는 것만 예외 */
```

숫자에는 `tabular-nums`. 카운트업 애니메이션 중 자릿수가 흔들리지 않아야 한다.

---

## 4. 형태

### 라운드 — 두 계열이 공존한다

**알약 스케일** (Nike). `rounded-sm/md/lg`로 사용.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--radius-sm` | 15px | 뱃지, 작은 칩 |
| `--radius-md` | 20px | 카드성 컨테이너 |
| `--radius-lg` | 30px | 알약 CTA — 시스템 표준 |

**패널 스케일** (Spotify 구조 차용). 인라인 `style`로 사용.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--radius-input` | 4px | 검색창 등 입력 필드 |
| `--radius-card` | 6px | 앨범아트, 박스형 카드 |
| `--radius-panel` | 8px | 섹션 패널, 시트, 다이얼로그 |

> 두 계열을 섞지 않는다. **버튼·칩은 알약, 면(surface)은 패널.**
> 리스트 행은 어느 쪽도 아니다 — 라운드 없음.

### 그림자

| 토큰 | 값 | 용도 |
|---|---|---|
| `--shadow-card` | `0 8px 8px rgba(0,0,0,0.3)` | 떠 있는 카드 |
| `--shadow-elevated` | `0 8px 24px rgba(0,0,0,0.5)` | 시트, 다이얼로그 |

무채색이다. 브랜드 컬러를 그림자에 섞지 않는다.

### 스페이싱

- **가로 거터: 24px (`px-6`)** — 모든 페이지 콘텐츠의 좌우 여백. 예외 없음.
- 섹션 간격: 40px (`mt-10`)
- 리스트 행: `px-6 py-5` (설정) / `px-6 py-4` (트랙 목록)
- 페이지 상단: `clamp(28px, 12dvh, 30dvh)` — 화면이 커도 30%를 넘지 않는다

---

## 5. 레이아웃

### 앱 셸

| 값 | |
|---|---|
| 논리 해상도 | **402 × 874** (iPhone 17) |
| 프레임 전환 브레이크포인트 | 768px (`md`) |
| 베젤 | 11px ring |
| 하단 탭바 | 68px + `env(safe-area-inset-bottom)` |

768px 이상에서는 폰 프레임 안에 담고, 뷰포트가 작으면 비율을 유지한 채
`transform: scale()`로 축소한다. 그 미만에서는 전체 화면.

### 스크롤 모델 — 중요

```css
html, body {
  height: 100%;
  overflow: hidden;
  overscroll-behavior: none;
}
```

**문서는 절대 스크롤되지 않는다.** 스크롤은 프레임 안쪽 `<main>`에서만 일어난다.
이걸 풀면 모바일에서 목록 끝을 당겼을 때 화면 전체가 고무줄처럼 들린다.

`body`에 `position: fixed`는 **쓰지 않는다.** iOS에서 키보드가 올라올 때 입력창이
키보드 뒤로 숨는다. 이 앱엔 검색·로그인·이름 편집 입력이 있어 부작용이 크다.

### 프레임 안의 `position: fixed`

프레임 내부 래퍼에 `transform: translateZ(0)`가 걸려 있다. 이게 없으면
바텀시트(`fixed inset-0`)의 기준이 바깥 스케일러가 돼서 폰 화면 밖으로 삐져나온다.
오버레이는 `absolute inset-0`을 쓰고, **애니메이션 클래스가 `position`을 덮어쓰지
않도록** 주의한다(§7 참고).

---

## 6. 컴포넌트

| 컴포넌트 | 스펙 |
|---|---|
| `PageHeader` | eyebrow(12 uppercase primary) + `.type-display` 44px + desc(14 mute) |
| `SectionTitle` | 16 semibold + 우측 `LinkAction` |
| `LinkAction` | 13 medium mute, `underline underline-offset-4` |
| `PillButton` | h-48 · `px-8` · `--radius-lg` · 15 semibold |
| `Chip` | h-36 · `px-4` · `--radius-lg` · 13 medium |
| `Row` | `px-6 py-5` · `border-b hairline-soft` · 라운드 없음 |
| `StatTile` | `bg-cloud` · `--radius-card` · 32px 숫자 + 12px 단위 |
| `ActionSheet` | 하단 시트 · `--radius-panel`(상단만) · `--shadow-elevated` |
| `Difficulty` | 5칸 · `3×7px` 막대 · 채움 primary / 빈 칸 hairline |

### 버튼 변형

| variant | 배경 | 텍스트 |
|---|---|---|
| `primary` | `bg-ink` | `text-canvas` |
| `brand` | `bg-primary` | `text-on-primary` |
| `secondary` | `bg-cloud` | `text-ink` |

**알약 CTA는 뷰포트당 하나.** 두 개 이상이면 무엇이 주 행동인지 사라진다.

### 아이콘

24px 그리드, `fill: none`, `stroke: currentColor`, **stroke-width 1.8**,
round cap/join. 채운 아이콘은 예외적으로만(하트 활성, Spotify 공식 마크).

---

## 7. 모션

### 곡선

| 곡선 | 쓰임 |
|---|---|
| `cubic-bezier(0.16, 1, 0.3, 1)` | **기본값.** 페이지 전환, 시트, 스태거, 확장 |
| `cubic-bezier(0.34, 1.56, 0.64, 1)` | 칩 — 오버슈트로 "선택했다"는 감각 |
| `cubic-bezier(0.4, 0, 0.7, 0.2)` | 상세 화면 나가기 — 들어올 때보다 급하게 |

### 지속 시간

| 이름 | 시간 | 설명 |
|---|---|---|
| `.page-in` | 500ms | 탭 이동 — 살짝 떠오름 |
| `.detail-in` | 500ms | 상세 진입 — 오른쪽에서 |
| `.detail-out` | 280ms | 상세 이탈 — 뒤로가기는 결과를 빨리 보고 싶은 동작 |
| `.overlay-in/out` | 500 / 280ms | 라우트가 아닌 **오버레이**용 (위치 속성 없음) |
| `.sheet-in` | 1100ms | 바텀시트 |
| `.scrim-in` | 900ms | 배경 딤 |
| `.pop-in` | 480ms | 드롭다운 |
| `item-in` | 420ms | 스태거 항목 |
| `bar-grow` | 620ms | 막대그래프 (`scaleY`) |
| `.row-expand` | 620ms | 필터 줄 펼침 |
| `.tap` | 520ms | 탭 피드백 (`scale(0.97)` + `opacity 0.6`) |
| `.hover-grow` | 180ms | 포인터 hover 확대 `scale(1.18)` |

> Tailwind의 `duration-*` 유틸리티는 globals.css에서 **전역 3배**로 늘려져 있다
> (`duration-200` → 600ms). 컴포넌트에 박힌 값을 일일이 고치지 않고 한 번에
> 느긋하게 만들기 위한 장치다. 짧은 hover 반응이 필요하면 `duration-*`을 쓰지
> 말고 전용 클래스를 만든다 — `.hover-grow`가 그 예다.

### 스태거

- `.stagger > *` — 리스트 항목, 32ms 간격, 11개까지 계단
- `.page-stagger > * > *` — 페이지 섹션, 0/70/140/200/250/290ms

`page-stagger`가 한 단계 더 내려가는(`> * > *`) 이유: PageTransition이 감싸는
div의 자식은 페이지 루트 div 하나뿐이라, 거기에 걸면 아무 일도 안 일어난다.

### 함정 — 애니메이션 클래스가 레이아웃을 건드릴 때

`.detail-in`은 **라우트 전체**가 문서 흐름 안에서 밀려 들어오는 것이라
`position: relative`를 같이 건다. 이걸 `absolute inset-0` 오버레이에 재사용하면
레이어 없는 CSS가 Tailwind `@layer utilities`를 이겨서 `absolute`가 죽고, 오버레이가
화면을 덮는 대신 콘텐츠 맨 끝에 이어붙는다. 그래서 위치를 건드리지 않는
`.overlay-in` / `.overlay-out`을 따로 둔다.

### 접근성

`prefers-reduced-motion: reduce`에서 모든 named animation을 끄고, `.chip`은
색 전환만 남긴다.

---

## 8. XR 씬 (3D)

앱 UI와 별개의 시각 언어. 벡터 라인만 쓴다.

| 요소 | |
|---|---|
| 탁구대 | `LineSegments` 와이어프레임 — 면 없음, 재질별 4 draw call |
| 안개 | `Fog(0x000000, 5.5, 17)` — 먼 라인이 어둠으로 잦아들며 원근을 만든다 |
| 공 | 흰색 / 강박은 오렌지 · 반투명 글로우 구 · 탁구대에 그림자 링 |
| 궤적 | 20점 라인, 가산 합성으로 꼬리를 어둡게 깎아 페이드 |
| 판정 색 | perfect `#f24822` · good `#ffffff` · miss `#6f6f6f` |

**색을 늘리지 않는 이유:** 파랑·초록을 추가하면 탁구대 라인과 싸워서 오히려
판정이 안 읽힌다. 앱 본체의 "무채색 + 오렌지" 규칙이 3D에서도 그대로다.

---

## 9. 베리에이션 가이드

### 바꿔도 되는 것

- **브랜드 색.** `--color-primary` 계열 4개만 갈면 전체가 따라온다. 액센트가
  하나뿐이라 교체 비용이 가장 싸다.
- **디스플레이 폰트.** Bebas Neue 자리는 굵고 좁은 대문자 그로테스크면 대체 가능.
  한글 폴백이 Pretendard 900이라는 점만 유지하면 된다.
- **라운드 스케일.** 알약(15/20/30)과 패널(4/6/8)을 각각 통째로 옮길 수 있다.
  단, 두 계열의 **대비**는 유지한다 — 알약은 확실히 둥글고 패널은 확실히 각져야
  구분이 산다.
- **모션 속도.** 곡선을 유지한 채 시간만 비례해 조절.
- **중립 팔레트의 온도.** 회색에 살짝 색을 섞어도 된다(웜/쿨 그레이). 명도 단계
  간격만 지키면 위계가 유지된다.

### 바꾸면 시스템이 무너지는 것

- **액센트를 둘 이상으로 늘리는 것.** 이 시스템은 "하나뿐인 색"에 강조를 전부
  걸고 있다. 두 번째 색이 들어오면 primary가 그냥 여러 색 중 하나가 된다.
- **리스트 행에 라운드/카드를 주는 것.** 엣지투엣지 플랫 리스트가 Nike 층위의
  핵심이다. 카드로 바꾸면 조밀한 정보 밀도가 사라진다.
- **가로 거터 24px.** 모든 화면이 이 축으로 정렬돼 있다. 페이지마다 다르면
  스크롤할 때 콘텐츠가 좌우로 흔들린다.
- **문서 스크롤 잠금 해제.** §5 참고 — 모바일에서 화면이 들린다.
- **큰 글자 자간을 양수로.** `.type-display`의 `-0.02em`은 한글 각진 글자 사이
  여백을 죽이는 값이다. 풀면 응축된 조형이 사라진다.
- **뷰포트당 알약 CTA 2개 이상.**

### 웹사이트로 옮길 때

이 시스템은 **402px 폭 앱 셸**을 전제로 만들어졌다. 넓은 화면으로 옮길 때:

1. **거터는 24px를 유지하되 콘텐츠에 max-width를 건다.** 거터를 늘려 대응하면
   모바일과 리듬이 달라진다.
2. **`.type-display` 44px는 데스크톱에서 작다.** 64–96px까지 올리고 자간을
   `-0.03em`쯤으로 더 조인다(큰 글자일수록 타이트하게 — §1-2).
3. **스크롤 잠금은 버린다.** 그건 앱 셸을 위한 장치지 웹 문서용이 아니다.
4. **탭바 → 상단 내비.** 68px 하단 바는 폰 전용이다.
5. **`.tap`(520ms)보다 `.hover-grow`(180ms)를 기본으로.** 포인터 환경에서는
   손가락을 뗀 뒤의 여운보다 즉각적인 hover 반응이 맞다.
6. **항상 어두운 면 규칙은 그대로 가져간다.** 사진 위 카드가 다크모드에서
   흰색으로 뒤집히는 사고를 막아준다.
